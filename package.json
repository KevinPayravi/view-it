{
  "name": "view-it",
  "version": "1.0.0",
  "description": "View it! tool and API for querying and displaying images using structured data on Wikimedia Commons.",
  "main": "server.js",
  "scripts": {
    "test": "env PORT=3000 nodemon server.js",
    "start": "node server.js",
    "deploy": "toolforge webservice --backend=kubernetes node24 restart",
    "logs": "kubectl logs -f $(kubectl get pods | tail -1 | cut -d ' ' -f 1)"
  },
  "repository": {
    "type": "git",
    "url": ""
  },
  "author": "Kevin Payravi (SuperHamster)",
  "license": "MIT",
  "homepage": "",
  "engines": {
    "node": ">=24"
  },
  "dependencies": {
    "cors": "^2.8.6",
    "express": "^5.2.1"
  },
  "devDependencies": {
    "eslint": "^10.3.0",
    "nodemon": "^3.1.14"
  }
}
